# v1.0.0

First official release of `silero-tts-api`.

## Highlights
- Stable OpenAI-compatible TTS API (`/health`, `/v1/models`, `/v1/audio/speech`)
- Production-ready quality gates (`lint`, `typecheck`, `tests`, docker build smoke)
- Reproducible local workflow with `make setup` and `make check`
- Security baseline with `pip-audit` checks
- Updated docs and architecture decisions

## Verification
- `make check` passed
- `make audit-deps` passed
- CPU Docker build smoke passed

## Docker images
- `ghcr.io/dexogen/silero-tts-api:cpu`
- `ghcr.io/dexogen/silero-tts-api:cuda`
